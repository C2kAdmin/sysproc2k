Options -Indexes

RewriteEngine On

# Si existe el archivo o carpeta físicamente, déjalo pasar
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Todo lo demás al puente (index.php)
RewriteRule ^ index.php [L]
